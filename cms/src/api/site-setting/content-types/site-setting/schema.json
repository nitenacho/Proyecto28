{
  "kind": "singleType",
  "collectionName": "site_settings",
  "info": {
    "singularName": "site-setting",
    "pluralName": "site-settings",
    "displayName": "Ajustes del sitio",
    "description": "Valores por defecto del Tweaks panel (logo, popup, cámara, HUD)."
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "defaultLogo": {
      "type": "enumeration",
      "default": "PROYECTO 28",
      "enum": ["PROYECTO 28", "NEIT.", "ESTUDIO 028"]
    },
    "defaultPopupPlacement": {
      "type": "enumeration",
      "default": "cursor",
      "enum": ["side", "cursor", "corner"]
    },
    "defaultTileStyle": {
      "type": "enumeration",
      "default": "cyan-copper",
      "enum": ["cyan-copper", "cyan-only", "copper-only", "mono"]
    },
    "cameraTilt": {
      "type": "integer",
      "default": 58,
      "min": 30,
      "max": 75
    },
    "cameraYaw": {
      "type": "integer",
      "default": 0,
      "min": -60,
      "max": 60
    },
    "cameraDrift": {
      "type": "boolean",
      "default": true
    },
    "showGrid": {
      "type": "boolean",
      "default": true
    },
    "showScanlines": {
      "type": "boolean",
      "default": false
    },
    "showViewfinder": {
      "type": "boolean",
      "default": true
    },
    "defaultGravityEnabled": {
      "type": "boolean",
      "default": true,
      "description": "Activa las físicas de gravedad y saltos de la luz controlable por defecto."
    },
    "gameLightSpeed": {
      "type": "decimal",
      "default": 8.0,
      "min": 1,
      "max": 30,
      "description": "Velocidad horizontal de la luz controlable (unidades/segundo)."
    },
    "gameLightJumpHeight": {
      "type": "decimal",
      "default": 3.0,
      "min": 0.5,
      "max": 10,
      "description": "Altura base del salto en unidades."
    },
    "gameLightJumpCount": {
      "type": "integer",
      "default": 4,
      "min": 1,
      "max": 8,
      "description": "Saltos consecutivos antes de tener que tocar piso (estilo Kirby)."
    },
    "gameLightGravity": {
      "type": "decimal",
      "default": 20.0,
      "min": 5,
      "max": 60,
      "description": "Aceleración gravitatoria aplicada a la luz."
    },
    "gameLightVelocityCurve": {
      "type": "enumeration",
      "default": "kirby",
      "enum": ["linear", "easeOut", "easeInOut", "kirby"],
      "description": "Curva de ramp-up de velocidad al presionar WASD."
    },
    "gameLightColor": {
      "type": "enumeration",
      "default": "cyan",
      "enum": ["cyan", "red", "green"],
      "description": "Color visual de la luz controlable desde Tweaks."
    },
    "gameMouseFollowDelay": {
      "type": "decimal",
      "default": 1.0,
      "min": 0,
      "max": 5,
      "description": "Segundos sin input WASD antes de que la luz vuelva a seguir el mouse."
    },
    "gameFallDuration": {
      "type": "decimal",
      "default": 1.0,
      "min": 0.2,
      "max": 3,
      "description": "Segundos de caída al vacío antes de respawnear en el cubo central."
    },
    "gameShadowSize": {
      "type": "decimal",
      "default": 0.3,
      "min": 0.15,
      "max": 1.2,
      "description": "Tamaño base del anillo de sombra bajo la luz controlable."
    },
    "audioEnabled": {
      "type": "boolean",
      "default": true,
      "description": "Activa o desactiva el sintetizador WebAudio de hover e interacciones."
    },
    "audioPreset": {
      "type": "enumeration",
      "default": "midi",
      "enum": ["midi", "glass", "soft"],
      "description": "Timbre del audio interactivo: teclado MIDI moderno, cristal o suave."
    },
    "audioMasterVolume": {
      "type": "decimal",
      "default": 0.24,
      "min": 0,
      "max": 1,
      "description": "Volumen master del audio interactivo."
    },
    "audioHoverVolume": {
      "type": "decimal",
      "default": 0.2,
      "min": 0,
      "max": 1,
      "description": "Volumen de notas al pasar sobre bloques."
    },
    "audioInteractionVolume": {
      "type": "decimal",
      "default": 0.18,
      "min": 0,
      "max": 1,
      "description": "Volumen de sonidos minimalistas de interacciones."
    },
    "adminButtonVisible": {
      "type": "boolean",
      "default": false,
      "description": "Toggle del botón secreto bajo el logo. Solo se cambia desde Tweaks por admin autenticado."
    },
    "pixelStreamingEnabled": {
      "type": "boolean",
      "default": false,
      "description": "Master switch global para Pixel Streaming. Si está OFF, ningún cubo monta stream."
    },
    "pixelStreamingPreviewEnabled": {
      "type": "boolean",
      "default": false,
      "description": "Muestra el overlay/preview de streaming aunque no exista una URL activa. Si está OFF, no aparece fallback sobre el cubo."
    },
    "pixelStreamingMode": {
      "type": "enumeration",
      "default": "shared",
      "enum": ["shared", "per-cube"],
      "description": "Una instancia Unreal compartida con switch de Level vs. una instancia por cubo (más costoso)."
    }
  }
}
